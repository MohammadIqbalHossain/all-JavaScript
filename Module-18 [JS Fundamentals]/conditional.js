
var bestQuote =  "Nothing is fucks us harder than time";
var quoteBySnow = "Duty is the death of love";

if (bestQuote == quoteBySnow) {
    console.log("Their thoughts are not same")
} else if (bestQuote != quoteBySnow) {
    console.log("Their words length are different")
} else if (bestQuote >= quoteBySnow || bestQuote != quoteBySnow) {
    console.log("We can't define theire thoughts with these two quotes")
}else {
    console.log("If we want to be best we have to secrifice love and time")
}