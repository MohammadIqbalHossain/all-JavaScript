
// Adding two number.
function addintion(num1, num2) {
    var add = num1 + num2;
    return add;
}
var firstNum = 48;
var secondNum = 889;

var result = addintion(secondNum, firstNum);
console.log(result);

// Multiplication
function Multiplication(firstCount, secondCount){
     var result = firstCount * secondCount;
     return result;
    
}

var gotAnswer = Multiplication(74, 68)
console.log(gotAnswer);


//Substraction 
function substraction(number1, number2) {
    var minus = number1 - number2;
    return minus;
}

var firstNumber = 74;
var secondNumber = 23;
var minused = substraction(secondNumber, firstNumber);
console.log(minused);

//Divition 
function divition(person1, person2) {
    var divided = person1 / person2;
    return divided;
}

var divide =  divition(32, 67);
console.log(divide);

// remainder: It's not working I have to google it.
function remainder(remain1, remain2) {
    var reminnig = remain1 % remain2;
    return reminnig 
}

var remainded = remainder(50, 3);
console.log(remainder);